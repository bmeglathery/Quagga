<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.bmeglathery.quagga.MainActivity">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" >

        <item
            android:id="@+id/bg_color"
            android:onClick="randomizeBackgroundColor"
            android:title="@string/randomize_background"/>

        <item
            android:id="@+id/reset_stats"
            android:onClick="deleteSavedResults"
            android:title="@string/delete_saved_results"/>

    </item>
</menu>
